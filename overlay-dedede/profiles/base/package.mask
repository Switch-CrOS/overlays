# TODO(b/302577077): Mask off dedede's linux-sources overlay.
# It's not longer needed, but removing it causes build issues because the binpkg
# is still pulled in.  Delete this by Nov 2023.
virtual/linux-sources::dedede
