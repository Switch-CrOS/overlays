# switch-t210: early-failure intentionally generates a SIGSEGV via
# `timeout --signal=SIGSEGV 0.01s sleep 10000` to exercise crash-reporter.
# Useless during bring-up and wastes ~500ms on a coredump path.
manual
