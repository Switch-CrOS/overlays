description "Switch delayed UI snapshots"
author "switchroot bring-up"
oom score -100
start on started ui
stop on stopping ui
console output
script
  sleep 15
  /usr/sbin/switch-log-snapshot ui-delayed-15 || true
  sleep 45
  /usr/sbin/switch-log-snapshot ui-delayed-60 || true
end script
