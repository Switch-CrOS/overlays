# switch-t210: auditd -n exits on bring-up (likely no audit kernel
# subsystem state to attach to, or /etc/audit ruleset issues), and upstart
# respawns it in a tight loop.  Nothing critical depends on auditd during
# bring-up; the kernel-side audit messages are still visible in dmesg if
# anyone needs them.
manual
