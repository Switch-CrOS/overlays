# nvpmodel persists its active mode (status) and the active conf path under
# /var/lib/nvpmodel.  Created here rather than with mkdir in the init script,
# per the CrOS init-script lint policy.
d /var/lib/nvpmodel 0755 root root
